#Inicio das anotações do video


#Variaveis = armazenam dados de tipos variados
texto = 'Isso é uma string'
idade = 26 #Isso é um Inteiro
peso  = 88.5 #Isso é um float (decimal)
trabalhando = True #Isso é um booleano (pode ser True ou False)

print(type(peso)) #Para verificar o tipo de uma variavel (Print imprime o valor que você quiser ver no console)


#Condicionais(IF) = decisões e validações de valores armazenados
    # Dois iguais "==" para validar igualdade, 1 igual "=" para atribuir valor
    #Elif = um if mas com uma condicional a mais para a validação
    #Caso nem o if, nem o elif forem True, ele vai para o Else


#Laços de repetição (loop)
#for itera por uma lista ou range delimitado (ou seja, você sabe quantas vezes vai fazer o loop)
#while pode ser um loop infinito enquanto a condição for true